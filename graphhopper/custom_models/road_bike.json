{
  "priority": [
    { "if": "road_class == PRIMARY || road_class == SECONDARY || road_class == TERTIARY || road_class == RESIDENTIAL", "multiply_by": "1.3" },
    { "if": "surface == PAVED || surface == ASPHALT || surface == PAVING_STONES", "multiply_by": "1.15" },
    { "if": "road_class == PATH || road_class == TRACK", "multiply_by": "0.55" },
    { "if": "road_environment == FERRY", "multiply_by": "0" }
  ]
}
