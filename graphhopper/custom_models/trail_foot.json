{
  "priority": [
    { "if": "road_class == PATH || road_class == TRACK || road_class == FOOTWAY || road_class == BRIDLEWAY", "multiply_by": "1.6" },
    { "if": "surface == UNPAVED || surface == GRAVEL || surface == GROUND || surface == DIRT || surface == GRASS", "multiply_by": "1.3" },
    { "if": "road_class == PRIMARY || road_class == SECONDARY || road_class == TERTIARY", "multiply_by": "0.35" },
    { "if": "road_class == RESIDENTIAL || road_class == SERVICE", "multiply_by": "0.7" },
    { "if": "road_environment == FERRY", "multiply_by": "0" }
  ]
}
